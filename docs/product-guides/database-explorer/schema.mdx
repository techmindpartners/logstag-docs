---
title: "Schema Tab: Reference"
sidebar_label: Schema
description: What the Database Explorer schema tab shows — object counts, the size treemap, and the object inventory — and how each is collected.
---

import ObjectCounts from '../_explain/database-explorer/schema/metric.object-counts.md';
import ValidObjects from '../_explain/database-explorer/schema/metric.valid-objects.md';
import InvalidObjects from '../_explain/database-explorer/schema/metric.invalid-objects.md';
import NaObjects from '../_explain/database-explorer/schema/metric.na-objects.md';
import TotalSize from '../_explain/database-explorer/schema/metric.total-size.md';
import ObjectSizes from '../_explain/database-explorer/schema/chart.object-sizes.md';
import DatabaseObjects from '../_explain/database-explorer/schema/table.database-objects.md';

# Schema Tab: Reference

This page is the source of truth for the in-app **Explain this** panels on the
[Database Explorer](/product-guides/database-explorer/) detail view's Schema tab.
Each section is written once as a content partial under `_explain/` and rendered both here
and inside the dashboard's info panel (`scripts/build-explain.mjs` compiles the registry).

The Schema tab is shown for PostgreSQL, Microsoft SQL Server, MongoDB, and Oracle. Redis and
Valkey are key-value stores without a schema model, so the tab is hidden for them — note that
their health score's Schema dimension still applies, scored from key-design signals.

## Object Counts {#object-counts}

<ObjectCounts />

## Valid {#valid-objects}

<ValidObjects />

## Invalid {#invalid-objects}

<InvalidObjects />

## N/A {#na-objects}

<NaObjects />

## Total Size {#total-size}

<TotalSize />

## Object Sizes {#object-sizes}

<ObjectSizes />

## Database Objects {#database-objects}

<DatabaseObjects />
